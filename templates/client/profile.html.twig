{% extends 'base.html.twig' %}

{% block body %}
    <br>
    <br>
    <br>
<div class="container">
    <h2>Profile client-login :  {{ client.login }}</h2>
    <div class="card bg-light">
        <div class="card-body">
            <div>Login: {{ client.login }}</div>
            <br>
            <div>Name: {{ client.firstname }}</div>
            <br>
            <div>Surname: {{ client.lastname }}</div>
            <br>
            <div>Registered: {{ client.createdAt|date("F jS \\a\\t g:ia") }}</div>
            <br>
            <div>Sex: {{ client.sex }} </div>
            <br>
            <div class="text-white bg-white">
                <a href="{{ path('update_client', {'client': client.id}) }}">Edit</a>
                <a href="{{ path('delete_client', {'client': client.id}) }}">Delete</a>
            </div>
            {#<div class="text-white bg-white"><a href="{{ path('delete_client', {'client': client.id}) }}">Delete</a></div>#}
        </div>
    </div>

    {#<h2>Comments</h2>#}
    {#{% for comment in article.comments %}#}
        {#<div class="card bg-success text-white">#}
            {#<div class="card-body">#}
                {#<div>Author: {{ comment.author }}</div>#}
                {#<br>#}
                {#<div>Created: {{ comment.createdAt|date("F jS \\a\\t g:ia") }}</div>#}
                {#<br>#}
                {#<div>Content: <br> {{ comment.content }}</div>#}
                {#<div class="text-white bg-white"><a href="{{ path('comment_update_form', {'article': article.id , 'comment':comment.id}) }}">Edit</a></div>#}
                {#<div class="text-white bg-white"><a href="{{ path('comment_delete', {'article': article.id, 'comment':comment.id}) }}">Delete</a></div>#}
            {#</div>#}
        {#</div>#}
    {#{% endfor %}#}
    {#{{ render(controller('App\\Controller\\CommentController::create',{'article':article.id})) }}#}

</div>

{% endblock %}
